requests>=2.31
psycopg2-binary>=2.9
python-dotenv>=1.0
google-api-python-client>=2.0
google-auth>=2.0
boto3>=1.34
# Notification layer (notify/) — rule + site config is YAML
PyYAML>=6.0

# StoreKit webhook receiver (separate always-on web service)
flask>=3.0
gunicorn>=21.0
# PINNED 2026-08-21: was `svix>=1.0`, so a rebuild silently pulled svix 2.0.0
# mid-incident. Pin the major version -- signature verification is not
# something we want changing underneath us on an unrelated redeploy.
svix>=1.0,<2
